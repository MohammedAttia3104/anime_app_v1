import 'package:flutter/material.dart';

class SingleVehiclesScreen extends StatelessWidget {
  const SingleVehiclesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
